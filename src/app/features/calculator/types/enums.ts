export enum OperatorEnum {
  Addition = '+',
  Subtraction = '−',
  Multiplication = '×',
  Division = '/',
  Wipe = '<',
  Clear = 'C',
  Equal = '=',
}

export enum NumpadEnum {
  Zero,
  One,
  Two,
  Three,
  Four,
  Five,
  Six,
  Seven,
  Eight,
  Nine,
}
